class Memo {
  Memo(this.id, this.title, this.date, this.name, this.team, this.mainText);
  String id;
  String title;
  String date;
  String name;
  String team;
  String mainText;
}

class Directory {
  Directory(this.id, this.year);
  String id;
  String year;
}
